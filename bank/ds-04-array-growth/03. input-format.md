The first line contains the number of test cases, `T`.

Each test case begins with a line holding the starting capacity `c` and the
number of operations `Q`, separated by a space.

The next `Q` lines each hold one operation: `add`, `addAt i`, or `removeAt i`.
