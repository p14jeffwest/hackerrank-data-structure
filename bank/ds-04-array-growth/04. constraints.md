- $1 \le T \le 100$
- $1 \le c \le 1000$
- $1 \le Q \le 200{,}000$, and the sum of `Q` over all test cases is at most
  200,000.
- Every position is valid at the moment it is used: the list is non-empty and
  $0 \le i < size$. You will not be given an out-of-range position.
- **The totals get large.** With 200,000 insertions at the front, the number
  of elements moved passes $10^{10}$.
- $O(Q)$ per test case is expected.
- The Java class must be named `Solution`.
- Keep your source file **plain ASCII**, comments included.
