The first line contains the count `N` and the integer `k`, separated by a
space.

The second line contains the `N` values, separated by spaces. They are in no
particular order, though they may happen to be sorted.
