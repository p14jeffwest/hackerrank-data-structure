- $1 \le T \le 500$
- $1 \le n \le 200{,}000$, and the sum of `n` over all test cases is at most
  200,000.
- Each element is an integer with $1 \le x \le 1{,}000{,}000{,}000$.
- **A majority element always exists**, so you never have to report its
  absence.
- $O(n)$ time per test case is expected, using $O(1)$ extra space.
- Leave the code above and below the marked region unchanged; write only the
  `majorityElement` method.
- Do not modify the list that is passed in.
- Do not substitute `ArrayList`, `LinkedList`, or any other ready-made
  collection for the list.
- Keep your source file **plain ASCII**, comments included.
