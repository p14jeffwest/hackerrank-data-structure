- $1 \le N \le 20$
- The number of moves printed reaches 1,048,575 lines. **The output is large,
  so gather it in a `StringBuilder` and write it once.**
- The starting peg is `A`, the target peg is `C`, and the spare is `B`.
- The Java class must be named `Solution`.
- Keep your source file **plain ASCII**, comments included.
