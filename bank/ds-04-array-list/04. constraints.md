- $1 \le Q \le 20{,}000$
- Every value `X` is an integer with $1 \le X \le 1{,}000{,}000{,}000$.
- A position `i` is an integer with $-1{,}000{,}000 \le i \le 1{,}000{,}000$.
  **Positions outside the list are given on purpose** and must print `error`.
- The same value may appear at several positions. `removeValue` removes only
  the **first** one, and `indexOf` reports only the **first** one.
- Commands may arrive while the list is empty.
- Leave the code above and below the marked region unchanged; write only the
  three methods.
- Do not substitute `ArrayList`, `LinkedList`, or any other ready-made
  collection for the list.
- Keep your source file **plain ASCII**, comments included.
