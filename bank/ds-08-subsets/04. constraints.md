- $1 \le T \le 50$
- $1 \le n \le 16$, and the total number of subsets over all test cases is at
  most 200,000.
- Each value is an integer with $1 \le x \le 1{,}000$, and **all values in one
  test case are distinct**.
- Solve it with recursion.
- The output is large; gather it in a `StringBuilder` and write it once.
- The Java class must be named `Solution`.
- Keep your source file **plain ASCII**, comments included.
