## Tutorial 2: Echo a Line

Read one line from standard input and print it back exactly.

Use `nextLine()` so the whole line is read, including spaces.

```java
Scanner sc = new Scanner(System.in);
String line = sc.nextLine();
System.out.println(line);
```

This is a simple read-and-print problem.
