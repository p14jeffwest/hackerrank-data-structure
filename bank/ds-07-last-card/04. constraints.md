- $1 \le N \le 500{,}000$
- Each number is an integer with $1 \le x \le 1{,}000{,}000{,}000$. **The same
  number may appear on several cards.**
- $O(N)$ overall is expected. A structure whose front removal is $O(N)$ will
  time out.
- The input and output are large; use `BufferedReader` and `StringBuilder`.
- The Java class must be named `Solution`.
- Keep your source file **plain ASCII**, comments included.
