- $1 \le T \le 500$
- Each tree has between 1 and 100,000 nodes, and the total over all trees is
  at most 200,000. **No tree is empty.**
- Each key is an integer with
  $-2^{31} \le key \le 2^{31} - 1$, so `Integer.MIN_VALUE` and
  `Integer.MAX_VALUE` both occur.
- **A tree containing two equal keys is not a BST.** The rule is strict on
  both sides.
- The height of any tree is at most 5,000, so a recursive solution will not
  run out of stack.
- Leave the code above and below the marked region unchanged; write only the
  `isValidBST` method (plus any helper of your own).
- Keep your source file **plain ASCII**, comments included.
