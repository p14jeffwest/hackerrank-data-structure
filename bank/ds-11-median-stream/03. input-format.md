The first line contains the number of commands, `M`.

Each of the next `M` lines contains one command.

| Command | Meaning |
|---|---|
| `add x` | One more number arrives. No output. |
| `median` | Print the median of everything so far. |
| `size` | Print how many numbers have arrived. |
