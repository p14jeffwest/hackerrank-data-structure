- $1 \le T \le 1{,}000$
- Each string is between 1 and 1,000 characters long.
- Strings contain only English letters and digits. There are no spaces.
- **Upper and lower case are different characters.**
- Solve it with recursion.
- Leave the code above and below the marked region unchanged; write only the
  `isPalindrome` method (plus any helper method of your own).
- Keep your source file **plain ASCII**, comments included.
