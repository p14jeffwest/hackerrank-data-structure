- The tree has between 1 and 100,000 nodes. **It is never empty.**
- Each node's value is an integer with $-1{,}000 \le v \le 1{,}000$.
  **Negative values occur**, so a running total can decrease.
- **The height of the tree is at most 5,000**, so a recursive solution will
  not run out of stack.
- $1 \le Q \le 1{,}000$
- Each target is an integer with
  $-5{,}000{,}000 \le target \le 5{,}000{,}000$.
- **A path runs from the root to a leaf.** A leaf has no children at all.
- Leave the code above and below the marked region unchanged; write only the
  `hasPathSum` method.
- Keep your source file **plain ASCII**, comments included.
