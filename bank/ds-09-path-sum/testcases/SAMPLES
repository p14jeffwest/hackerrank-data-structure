# Case numbers to mark as "sample" on HackerRank, one per line.
# 00 the worked example
# 01 a single node, which is itself a leaf
# 02 a chain of one-sided nodes: exactly one leaf, so exactly one path sum.
#    The targets include every PARTIAL sum along the way -- 1, 3, 6, 10 --
#    which can only be reached by stopping short of the leaf. 02 must be
#    published: a solution that answers true at a missing child says true to
#    all of them.
00
01
02
