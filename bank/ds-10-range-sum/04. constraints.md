- The tree has between 1 and 200,000 nodes. **It is never empty, and it is
  always a valid BST with distinct keys.**
- Each key is an integer with
  $-1{,}000{,}000{,}000 \le key \le 1{,}000{,}000{,}000$.
- The height of the tree is at most 5,000, so a recursive solution will not
  run out of stack.
- $1 \le Q \le 200{,}000$, and every query satisfies $low \le high$.
- **A single answer can reach about $2 \times 10^{14}$**, which an `int`
  cannot hold.
- Leave the code above and below the marked region unchanged; write only the
  `rangeSum` method.
- Keep your source file **plain ASCII**, comments included.
