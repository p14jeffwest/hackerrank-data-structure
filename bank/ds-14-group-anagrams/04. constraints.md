- $1 \le n \le 100{,}000$
- Every word is 1 to 20 lower-case English letters. **The same word may appear
  more than once**, and identical words are anagrams of each other.
- $O(n k \log k)$ is expected, for words of length `k`.
- Leave the code above and below the marked region unchanged; write only the
  `groupAnagrams` method.
- Keep your source file **plain ASCII**, comments included.
