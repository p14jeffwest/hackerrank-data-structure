- $1 \le n \le 100{,}000$
- Each value is an integer with $0 \le v \le 1{,}000{,}000{,}000$. **The same
  value may appear more than once, and every value may be 0.**
- The answer can reach about 900,000 digits, so **no numeric type holds it**.
- $O(n \log n)$ is expected.
- Leave the code above and below the marked region unchanged; write only the
  `largestNumber` method.
- Keep your source file **plain ASCII**, comments included.
