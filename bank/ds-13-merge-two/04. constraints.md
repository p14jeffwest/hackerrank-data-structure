- $0 \le n$, $0 \le m$, and $1 \le n + m \le 400{,}000$. **Either array may be
  empty, but not both.**
- Both arrays are given in **ascending order**.
- Each value is an integer with
  $-1{,}000{,}000{,}000 \le v \le 1{,}000{,}000{,}000$. **Values may repeat,
  within an array and across the two.**
- $O(n + m)$ is expected. Do not call a sort library.
- Leave the code above and below the marked region unchanged; write only the
  `mergeSorted` method.
- Keep your source file **plain ASCII**, comments included.
