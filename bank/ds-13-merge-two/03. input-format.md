The first line contains the length of the first array, `n`, and the second
line its `n` values in ascending order.

The next two lines give the second array the same way: its length `m`, then
its `m` values.

**An array of length 0 has no values line at all.**
