- $1 \le n \le 200{,}000$
- $0 \le start \le end \le 1{,}000{,}000{,}000$. **`start` may equal `end`,
  and the same interval may appear more than once.**
- Two intervals sharing only an endpoint **do** overlap and must be merged.
- $O(n \log n)$ is expected.
- Leave the code above and below the marked region unchanged; write only the
  `merge` method.
- Keep your source file **plain ASCII**, comments included.
