- $1 \le T \le 500$
- $1 \le n \le 200{,}000$, and the sum of `n` over all test cases is at most
  200,000.
- $1 \le k \le n$, so the node you are asked for always exists.
- Each data value is an integer with
  $-1{,}000{,}000{,}000 \le x \le 1{,}000{,}000{,}000$. Values may repeat, and
  may be zero or negative.
- $O(n)$ time and $O(1)$ extra space per test case are expected.
- Leave the code above and below the marked region unchanged; write only the
  `kthFromEnd` method.
- Keep your source file **plain ASCII**, comments included.
