The first line contains the number of test cases, `T`.

Each test case is two lines:

- a line with the list length `n` and the integer `k`, separated by a space
- a line with the `n` data values, from the first node to the last, separated
  by spaces
