- $1 \le T \le 500$
- $0 \le n \le 200{,}000$, and the sum of `n` over all test cases is at most
  200,000.
- Each value is an integer with
  $-100{,}000{,}000 \le x \le 100{,}000{,}000$, so ten times a value still
  fits in an `int`. **Values may be zero or negative.**
- $O(n)$ per test case is expected.
- Leave the code above and below the marked region unchanged; write only the
  `transform` method.
- Keep your source file **plain ASCII**, comments included.
