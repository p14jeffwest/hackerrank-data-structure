The first line contains the number of test cases, `T`.

Each test case is two lines:

- a line with the list length `n`
- a line with the `n` values, in order, separated by spaces

**A list of length 0 is given as an empty line**, which is still present in the
input and still has to be read.
