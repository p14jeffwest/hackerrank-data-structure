- $1 \le M \le 200{,}000$
- At most 20 of the commands are `print`.
- Every value is an integer with
  $-1{,}000{,}000{,}000 \le x \le 1{,}000{,}000{,}000$. **The same value may
  be pushed more than once.**
- **`pop` and `peek` are called on an empty heap on purpose.**
- `push` and `pop` must each run in $O(\log n)$.
- Leave the code above and below the marked region unchanged; write only the
  two methods.
- Do not substitute `PriorityQueue` or any other ready-made structure.
- Keep your source file **plain ASCII**, comments included.
