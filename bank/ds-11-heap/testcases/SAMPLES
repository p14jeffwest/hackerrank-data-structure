# Case numbers to mark as "sample" on HackerRank, one per line.
# 00 the trace from 11.5 Level 1 Problem 1: inserting 5, 3, 8, 1, 6
# 01 the trace from 11.5 Level 1 Problem 2: one pop from [1, 3, 8, 5, 6]
# 02 the down-heap example of 11.3, plus an empty heap touched at both ends.
#    02 must be published: the root's children are 4 and 6, and swapping with
#    6 instead of 4 leaves a broken array that still pops a plausible value
#    next time.
00
01
02
