- $1 \le C \le 100{,}000$
- $1 \le Q \le 200{,}000$
- Every value `X` is an integer with $1 \le X \le 1{,}000{,}000{,}000$.
- **`dequeue` and `front` are called on an empty queue on purpose, and
  `enqueue` is called on a full one.**
- The capacity never changes. The queue does not grow.
- `enqueue`, `dequeue` and `getFront` must each run in $O(1)$.
- Leave the code above and below the marked region unchanged; write only the
  three methods.
- Do not substitute `ArrayDeque`, `LinkedList`, or any other ready-made
  collection for the queue.
- Keep your source file **plain ASCII**, comments included.
