The first line contains the number of test cases, `T`.

Each test case begins with a line holding the number of meetings `n`, followed
by `n` lines each holding a start time and an end time separated by a space.
