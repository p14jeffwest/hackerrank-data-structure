- $1 \le T \le 2{,}000$
- The total number of tokens over all expressions is at most 300,000.
- Each operand is an integer with $-1{,}000 \le x \le 1{,}000$.
- Every expression is **valid**: an operator never runs short of operands, and
  exactly one value remains at the end.
- **No division by zero occurs.**
- Every intermediate result satisfies $|v| \le 10^9$, so an `int` is enough.
- $O(N)$ in the number of tokens is expected.
- The Java class must be named `Solution`.
- Keep your source file **plain ASCII**, comments included.
