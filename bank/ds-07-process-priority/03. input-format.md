The first line contains the number of test cases, `T`.

Each test case is two lines:

- a line with the number of processes `N` and the starting position `target`
  of the process to track. **Positions start at 0.**
- a line with the `N` priorities, in queue order

