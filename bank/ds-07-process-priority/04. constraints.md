- $1 \le T \le 100$
- $1 \le N \le 1{,}000$, and the sum of `N` over all test cases is at most
  20,000.
- $0 \le target < N$
- Each priority is an integer with $1 \le p \le 1{,}000{,}000$.
- **Only a strictly higher priority sends a process to the rear.** Equal
  priorities do not displace each other.
- The Java class must be named `Solution`.
- Keep your source file **plain ASCII**, comments included.
