- $1 \le M \le 200{,}000$
- At most 100,000 of the commands are `insert`, and at most 10 in total are
  `print` or `preorder`.
- Every key is an integer with
  $-1{,}000{,}000{,}000 \le x \le 1{,}000{,}000{,}000$.
- **The height of the tree never exceeds 5,000**, so a recursive solution
  will not run out of stack.
- When removing a node with two children, promote **the largest key in its
  left subtree**.
- Leave the code above and below the marked region unchanged; write only the
  three methods.
- Do not substitute `TreeMap`, `TreeSet`, or any other ready-made collection
  for the tree.
- Keep your source file **plain ASCII**, comments included.
