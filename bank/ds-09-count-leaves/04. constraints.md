- The tree has between 1 and 100,000 nodes. **It is never empty.**
- Each node's value is an integer with
  $-1{,}000{,}000{,}000 \le v \le 1{,}000{,}000{,}000$.
- **The height of the tree is at most 5,000**, so a recursive traversal will
  not run out of stack.
- **A leaf has no children at all.** A node with one child is not a leaf.
- `height` counts **edges**: a single node is 0, an empty subtree is -1.
- `maxDepth` counts **nodes**: a single node is 1, an empty subtree is 0.
- Leave the code above and below the marked region unchanged; write only the
  three methods.
- Keep your source file **plain ASCII**, comments included.
