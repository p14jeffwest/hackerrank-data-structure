# Problem metadata. This is a bookkeeping record; it is not entered into
# HackerRank directly. The files pasted into the form are the ones numbered
# 00 through 06.

id: ds-tutorial-03-sum

# Which set this belongs to: tutorial | ch03 ... ch14
set: tutorial

# Which chapter of the course book (wikidocs 20475) this comes from.
# The tutorial set has no chapter. The reading half comes from appendix C.5;
# the type half comes from the "The Range of Integers and long" TIP in B.1.
chapter: null

# page_id of the source section.
# C.5 Input and Output (Scanner, BufferedReader, and Fast I/O)
source_page_id: 375896

# Secondary source, cited in the statement.
# B.1 Variables, Data Types, and Operators (Static Typing)
also_cites: 375886

# Title shown on HackerRank
title: "Tutorial 3: Sum of N Integers"

# tutorial | easy | medium | hard
difficulty: tutorial

# Score. tutorial 5 / easy 10 / medium 30 / hard 50
max_score: 5

# Keep only this language enabled on HackerRank.
languages:
  - java15

# Does this problem use a custom Head/Body/Tail code stub?
custom_code_stub: false

# Has it been published on HackerRank?
published: false

# Has it been added to the contest via Add Challenge?
in_contest: false

# HackerRank slug assigned after publishing. The full URL is:
#   https://www.hackerrank.com/contests/<contest_slug>/challenges/<hackerrank_slug>
contest_slug: null
hackerrank_slug: null
url: null

# Corresponding problem in the Korean repo.
ko_counterpart: dsa-tutorial-03-sum

# Ordering note. The three tutorials must be solved in order.
pair_with:
  - ds-tutorial-01-hello
  - ds-tutorial-02-echo

# Verification record
verified:
  jdk: "21.0.10"           # compiled with `javac --release 15 -Xlint:all`
  release_target: 15
  warnings: 0
  testcases_passed: 15
  date: "2026-08-09"
  scored_variants:
    - code: "BufferedReader + StringTokenizer, long sum  (the registered solution)"
      score: "15/15"
      slowest_case: "222 ms (case 12, N = 100,000)"
    - code: "Scanner, long sum"
      score: "15/15"
      slowest_case: "479 ms (case 10, N = 100,000)"
    - code: "BufferedReader, int sum   (the intended mistake)"
      score: "9/15"
  notes: >
    The int accumulator fails 02, 07, 10, 11, 12 and 14 and passes the other
    nine, so the mistake surfaces as a partial score rather than a zero.
    Case 02 is the one to look at: three values of 1,000,000,000 come back as
    -1294967296, a negative answer from three positive inputs. It is published
    as a sample so the student meets that before submitting.
    Scanner runs roughly twice as slow as BufferedReader on the largest cases
    but stays well inside the limit at N = 100,000, which matches what the
    statement claims. Do not raise N without rechecking that claim.
    Largest input file is about 1.0 MB (case 12).
