- $1 \le size \le 1{,}000$ and $1 \le M \le 200{,}000$
- At most 20 of the commands are `print`.
- Keys and values are integers with
  $-1{,}000{,}000{,}000 \le k, v \le 1{,}000{,}000{,}000$. **Keys are often
  negative.**
- **`remove` is called on keys that are not in the table**, and `get` on keys
  that were never stored.
- A value of `-1` may be stored, and `get` cannot then distinguish it from a
  miss. That is this problem's convention.
- Use `key % size` as the hash function, and handle negative keys.
- Leave the code above and below the marked region unchanged; write only the
  four methods.
- Do not substitute `HashMap` or any other ready-made structure.
- Keep your source file **plain ASCII**, comments included.
