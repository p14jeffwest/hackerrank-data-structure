- $1 \le n \le 1{,}000{,}000$
- $0 \le maxValue \le 1{,}000{,}000$
- Every value is an integer with $0 \le v \le maxValue$. **A value may occur
  many times, and some values may not occur at all.**
- **`maxValue` itself may occur**, and so may `0`.
- $O(n + maxValue)$ is expected. **Do not use a comparison sort**
  (`Arrays.sort` and the like).
- Leave the code above and below the marked region unchanged; write only the
  `countingSort` method.
- Keep your source file **plain ASCII**, comments included.
