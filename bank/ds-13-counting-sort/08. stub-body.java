
    // Write the method below.
    //
    //   countingSort(a, maxValue) : the values of `a` in ascending order.
    //
    // Every value is between 0 and maxValue. Count how many times each value
    // occurs and use those counts to place the values, rather than comparing
    // them with one another.
    //
    // O(n + maxValue) is expected.

    static int[] countingSort(int[] a, int maxValue) {
        // TODO
        return new int[a.length];
    }
