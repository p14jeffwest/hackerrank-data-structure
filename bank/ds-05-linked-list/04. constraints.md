- $1 \le Q \le 10{,}000$
- Every value `X` is an integer with $1 \le X \le 1{,}000{,}000{,}000$.
- A position `i` is an integer with $-1{,}000{,}000 \le i \le 1{,}000{,}000$.
  **Positions outside the list are given on purpose** and must print `error`.
- `addAt i` is valid for $0 \le i \le size$; `removeAt i` and `get i` are
  valid for $0 \le i < size$.
- The same value may appear in several nodes. `removeValue` removes only the
  **first** one, and `indexOf` reports only the **first** one.
- Commands may arrive while the list is empty.
- **Do not create nodes you do not need**, and do not copy data between nodes
  to simulate a move. Insertion and removal are link changes.
- Leave the code above and below the marked region unchanged; write only the
  three methods.
- Do not substitute `ArrayList`, `java.util.LinkedList`, or any other
  ready-made collection.
- Keep your source file **plain ASCII**, comments included.
