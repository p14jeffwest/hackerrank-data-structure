- $1 \le T \le 500$
- $1 \le n \le 200{,}000$, and the sum of `n` over all test cases is at most
  200,000.
- $1 \le k \le 1{,}000{,}000{,}000$. **`k` is often far larger than `n`.**
- Each element is an integer with $1 \le x \le 1{,}000{,}000{,}000$. The same
  value may appear more than once.
- $O(n)$ per test case is expected.
- Leave the code above and below the marked region unchanged; write only
  the `rotate` method.
- Do not substitute `ArrayList`, `LinkedList`, or any other ready-made
  collection for the list.
- Keep your source file **plain ASCII**, comments included.
