The first line contains the number of test cases, `T`.

Each test case is two lines:

- a line with the list length `n` and the rotation amount `k`, separated by a
  space
- a line with the `n` elements of the list, in order, separated by spaces
