- $1 \le Q \le 200{,}000$
- Every value `X` is an integer with $1 \le X \le 1{,}000{,}000{,}000$.
- **`pop` and `peek` are called on an empty stack on purpose.**
- `push`, `pop` and `peek` must each run in $O(1)$, amortized for `push`.
- Leave the code above and below the marked region unchanged; write only the
  three methods.
- Do not substitute `ArrayDeque`, `java.util.Stack`, or any other ready-made
  collection for the stack.
- Keep your source file **plain ASCII**, comments included.
