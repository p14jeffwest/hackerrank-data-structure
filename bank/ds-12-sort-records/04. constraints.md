- $1 \le N \le 100{,}000$
- A name is 1 to 10 lower-case English letters. **The same name may appear
  more than once.**
- A score is an integer with $0 \le score \le 1{,}000$, so **ties are common**.
- Leave the code above and below the marked region unchanged; write only the
  `sortByScore` method.
- Keep your source file **plain ASCII**, comments included.
