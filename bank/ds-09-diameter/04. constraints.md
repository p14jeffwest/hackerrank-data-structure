- The tree has between 1 and 500,000 nodes. **It is never empty.**
- Each node's value is an integer with
  $-1{,}000{,}000{,}000 \le v \le 1{,}000{,}000{,}000$. **The values play no
  part in the answer.**
- **The height of the tree is at most 5,000**, so a recursive solution will
  not run out of stack.
- The diameter is counted in **edges**. A single node has diameter 0.
- $O(n)$ is expected.
- The input is large; the reading is already written for you, but keep your
  own work linear.
- Leave the code above and below the marked region unchanged; write only the
  `diameter` method (plus any helper of your own).
- Keep your source file **plain ASCII**, comments included.
