- $1 \le T \le 500$
- $1 \le n \le 500{,}000$, and the sum of `n` over all test cases is at most
  500,000.
- $1 \le k \le n$
- Each value is an integer with
  $-1{,}000{,}000{,}000 \le x \le 1{,}000{,}000{,}000$. **Values may repeat,
  and may be zero or negative.**
- $O(n)$ per test case is expected. $O(nk)$ will time out.
- The input and output are large; use `BufferedReader` and `StringBuilder`.
- The Java class must be named `Solution`.
- Keep your source file **plain ASCII**, comments included.
