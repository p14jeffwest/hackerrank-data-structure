The first line contains the number of test cases, `T`.

Each test case is two lines:

- a line with the array length `n` and the window size `k`, separated by a
  space
- a line with the `n` values, in order, separated by spaces
