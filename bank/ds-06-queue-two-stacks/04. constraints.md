- $1 \le Q \le 200{,}000$
- Every value `X` is an integer with $1 \le X \le 1{,}000{,}000{,}000$.
- **`dequeue` is called on an empty queue on purpose.**
- `dequeue` must be $O(1)$ **amortized**, so the whole run is $O(Q)$.
- **Use only the two stacks provided.** Do not add an array, a list, or any
  other collection, and do not use `java.util.Queue` or `ArrayDeque`.
- Leave the code above and below the marked region unchanged; write only the
  three methods.
- Keep your source file **plain ASCII**, comments included.
