- $1 \le T \le 500$
- $1 \le n \le 300{,}000$, and the sum of `n` over all test cases is at most
  300,000.
- Each temperature is an integer with $30 \le t \le 100$. **Repeated values
  are common**, and a day of equal temperature does not count as warmer.
- $O(n)$ per test case is expected. $O(n^2)$ will time out.
- The input and output are large; use `BufferedReader` and `StringBuilder`.
- The Java class must be named `Solution`.
- Keep your source file **plain ASCII**, comments included.
