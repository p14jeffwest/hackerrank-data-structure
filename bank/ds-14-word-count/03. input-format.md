The first line contains the number of words in the list, `N`.

The second line contains the `N` words, separated by spaces.

The third line contains the number of queries, `Q`.

The fourth line contains the `Q` query words, separated by spaces.

### Example

```
6
apple banana apple cherry banana apple
4
apple banana cherry durian
```
